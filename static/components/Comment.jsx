const Comment = (props) => {
    const [comment, setComment] = React.useState('');
    console.log("comment props", props)
    
    // React.useEffect(() =>{
    //     setComment(props.newComment['newComment']);

    // }, []);

   
    // return (
        // {props.text}
    // )
}